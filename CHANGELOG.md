# 更新日志

## v1.1.0（2026-08-15）

### ✨ 新增功能

- **继承外部 AI 规则**：设置页新增「🤝 继承外部 AI 规则」——自动搜索 Claude Code / Cursor / Gemini / Codex / Copilot 的规则文件，确认后读取并用 AI 精简改写（剔除该软件专属特性，如 plan!/ponytail/autosolve 等），追加写入 dsh 长期记忆（`~/.dsh/AGENTS.md`），跨会话生效
- **静默重启服务**：设置页右下角新增「🔄 重启 dsh 服务」按钮，一键 kill + 重新拉起 dsh web（改插件/补丁后强制生效，无需退出客户端）
- **插件市场与对话导入**：默认集成 `dsh-plugin-marketplace`（设置页浏览/搜索/安装社区插件）与 `dsh-chat-import`（六大工具历史对话导入），首次启动自动安装
- **插件介绍中文翻译**：插件市场详情面板新增「🌐 翻译为中文」按钮（经主进程代理 Google 翻译，无需翻墙接口）
- **同步确认选项**：启用同步前必须勾选「我已确认目标地址可信，知晓同步到云端存在泄露风险」
- **WebDAV/Git 连接测试**：配置区新增「测试连接」按钮，验证地址、认证与读写权限
- **备份包含插件与插件设置**：快照备份打包 `@linxin666` 全家桶插件本体、`cordis` 配置与 `pnpm` 锁定文件，恢复后开箱即用
- **作者署名**：设置页标题与底部、托盘「关于」弹窗展示「喵筱曦 · 开源免费使用（MIT）」
- **自动同步即时生效**：修改同步配置后定时器立即按新间隔刷新

### 🐛 修复

- **dsh-web-ui 安装即使用**：修复安装包装完插件未集成的缺陷——启动时自动检测并安装 pnpm、安装全家桶插件、打「概览」标签补丁（幂等、失败不阻塞）
- **WebDAV 全面修复**：
  - href 解析兼容相对/根相对/绝对三种形式（Synology 返回根相对路径不再错位）
  - 中文路径（如「杂货铺」）自动 URL 编码
  - 顶层文件（`.credentials.yaml`）上传时父目录计算错误
  - 远程会话多级目录递归拉取（原 depth-1 只列一层导致 pull 失效）
  - 目录创建/认证失败不再静默吞错，测试连接如实反馈
- **插件市场崩溃事故**：`dsh-plugin-marketplace` 误注册进 `dsh.profile.bundles` 导致 dsh 启动校验崩溃（"dsh web 启动超时"）——新增 bundle manifest 校验 + 启动自愈清理（`pruneInvalidBundles`），并自动按 README 正确挂载（cordis.patch.yml insert）
- **设置页按钮合并**：WebDAV 模式仅保留「保存设置 + 备份管理」，移除与备份冲突的「立即同步/从云端恢复」（Git 模式保留）
- **备份列表状态**：WebDAV 未配置时明确显示「✗ WebDAV 未配置」

### 🔧 其他

- 补丁脚本 `patch-aionui.js` 改为基于 `%USERPROFILE%` 动态路径（不再硬编码用户名）
- 手动更新脚本自动检测/安装 pnpm
- 打包跳过 native rebuild（纯 JS 应用，`npmRebuild: false`）

---

## v1.0.0（初始版本）

- 窗口内嵌 DSH Web UI、底部状态栏（模型/用量/费用/余额）
- 右侧面板「概览」标签、归档会话取消归档
- 托盘常驻、关闭最小化到任务栏、退出自动停止服务
- 云端快照备份（WebDAV/Git）、一键安装（Node.js/dsh 环境自检）
- dsh-web-ui 全家桶（皮肤中心 / 任务看板 / 宠物）集成

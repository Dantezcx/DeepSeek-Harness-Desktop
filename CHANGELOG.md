# 更新日志

## v1.2.0（2026-08-17）

### 🚀 性能

- **命令执行提速 70 倍**：修复 DSH 终端暗号不一致 Bug——`dsh-terminal-bash` 的 `CONTROLLED_PROMPT`（`dsh> `）与上层 `dsh-tool-bash-persistent` 设置的 PS1（`__DSH_PERSISTENT_BASH_PROMPT__`）对不上，导致简单命令每次都要等 3.5 秒静默超时兜底。客户端启动时自动统一暗号（幂等补丁），命令执行从 ~3600ms 降到 ~150ms，原生 50ms 快速通道恢复。
- 补丁随启动自动重打：dsh 全局包升级被覆盖后，下次启动自动修复，无需手动干预。

## v1.1.10（2026-08-17）

### 🐛 修复

- **静默更新崩溃**：修复 doUpdate 中局部变量 `path`（下载 URL）遮蔽 Node `path` 模块导致的 `path.join is not a function`——更新流程恢复正常

## v1.1.9（2026-08-17）

### ✨ 新增

- **静默更新**：状态栏新增版本号显示与「更新」按钮——检测 GitHub Releases 新版本 → 确认后自动下载（直连 + ghproxy/ghfast/gh-proxy 镜像兜底）→ NSIS 静默安装 → 重新打开即新版（多设备可从客户端直接更新）
- **版本号**：状态栏右下角显示当前版本（v1.1.9）

### 🐛 修复

- **会话费用离谱**：修复费用取历史峰值卡死 + tokens 分别取最大虚高；页面未提供缓存命中数时按缓存命中价（¥0.5/1M）估算，贴近 DeepSeek 真实账单（原按全价 ¥2/1M 虚高最多 4 倍）；「本次费用」改为增量计算
- **测试连接卡死**：WebDAV 测试超时 15s → 8s，服务器不可达时快速报错（配合界面异常保护）
- **备份列表误报**：服务器不可达/认证失败时如实显示错误，不再伪装成「暂无备份」
- **WebDAV 修复合入**：中文路径编码、href 三种形式解析、递归拉取、备份含插件与插件设置、davMkcol 状态检查
- **重启服务按钮修复**：接通 service:restart IPC（此前按钮调用了不存在的接口）

## v1.1.8（2026-08-15）

### 🐛 修复

- **同步测试连接卡死**：preload 补全 testSync API + 界面异常保护

## v1.1.7（2026-08-15）

### 🐛 修复

- **自动同步即时生效**：保存同步配置后重新调度定时器，无需重启客户端

## v1.1.6（2026-08-15）

### 🐛 修复与优化

- **插件市场 AI 解释修复**：README 获取改走**主进程代理**（国内可直连 GitHub API），不再出现"AI 解释无内容"
- **规则扫描新增 DSH 规则源**：除 Claude Code / Cursor 等外，自动识别 ~/.dsh/AGENTS.md、REASONIX.md
- **一键安装实时进度**：下载/安装过程心跳式刷新提示，不再疑似卡死
- **安装完成自动跳转**：依赖安装完毕后自动进入主界面
- **智能安装增强**：插件市场一键安装自动降级（npm → GitHub 镜像 ghproxy.net / ghfast.top / gh-proxy.com）

## v1.1.4（2026-08-15）

### 🐛 修复

- **AI 解释返回字符串修复**：修复 DeepSeek 响应被识别为对象导致界面显示 [object Object]
- **插件市场补丁**：AI 文案汉化、搜索走主进程代理、智能安装（npm 缺失时 Git 镜像兜底）

## v1.1.3（2026-08-15）

### ✨ 新增

- **插件 AI 解释**：插件市场详情页「🌐 AI 解释」调用 DeepSeek 生成中文功能说明
- **服务常驻**：关闭客户端窗口默认保持 dsh 服务运行（托盘可完全退出）
- **开机自启静默模式**：后台自启不弹终端窗口

## v1.1.2（2026-08-15）

### 🐛 修复

- **连接测试卡死**：补全 sync:test 处理器
- **恢复备份自愈**：恢复后自动清除无效 bundle，防止 DSH 离线崩溃

## v1.1.1（2026-08-15）

### ✨ 新增

- **一键安装实时进度**（心跳防卡死）
- **安装完成自动进入主界面**
- **自愈 marketplace 无效 bundle**（防止 DSH 离线崩溃）

## v1.1.0（2026-08-15）

### ✨ 新增功能

- **继承外部 AI 规则**：设置页新增「🤝 继承外部 AI 规则」——自动搜索 Claude Code / Cursor / Gemini / Codex / Copilot 的规则文件，确认后读取并用 AI 精简改写（剔除该软件专属特性，如 plan!/ponytail/autosolve 等），追加写入 dsh 长期记忆（`~/.dsh/AGENTS.md`），跨会话生效
- **静默重启服务**：设置页右下角新增「🔄 重启 dsh 服务」按钮，一键 kill + 重新拉起 dsh web（改插件/补丁后强制生效，无需退出客户端）
- **插件市场与对话导入**：默认集成 `dsh-plugin-marketplace`（设置页浏览/搜索/安装社区插件）与 `dsh-chat-import`（六大工具历史对话导入），首次启动自动安装
- **插件介绍中文翻译**：插件市场详情面板新增「🌐 翻译为中文」按钮（经主进程代理 Google 翻译，无需翻墙接口）
- **同步确认选项**：启用同步前必须勾选「我已确认目标地址可信，知晓同步到云端存在泄露风险」
- **WebDAV/Git 连接测试**：配置区新增「测试连接」按钮，验证地址、认证与读写权限
- **备份包含插件与插件设置**：快照备份打包 `@linxin666` 全家桶插件本体、`cordis` 配置与 `pnpm` 锁定文件，恢复后开箱即用
- **作者署名**：设置页标题与底部、托盘「关于」弹窗展示「喵筱曦 · 开源免费使用（MIT）」
- **自动同步即时生效**：修改同步配置后定时器立即按新间隔刷新

### 🐛 修复

- **dsh-web-ui 安装即使用**：修复安装包装完插件未集成的缺陷——启动时自动检测并安装 pnpm、安装全家桶插件、打「概览」标签补丁（幂等、失败不阻塞）
- **WebDAV 全面修复**：
  - href 解析兼容相对/根相对/绝对三种形式（Synology 返回根相对路径不再错位）
  - 中文路径（如「杂货铺」）自动 URL 编码
  - 顶层文件（`.credentials.yaml`）上传时父目录计算错误
  - 远程会话多级目录递归拉取（原 depth-1 只列一层导致 pull 失效）
  - 目录创建/认证失败不再静默吞错，测试连接如实反馈
- **插件市场崩溃事故**：`dsh-plugin-marketplace` 误注册进 `dsh.profile.bundles` 导致 dsh 启动校验崩溃（"dsh web 启动超时"）——新增 bundle manifest 校验 + 启动自愈清理（`pruneInvalidBundles`），并自动按 README 正确挂载（cordis.patch.yml insert）
- **设置页按钮合并**：WebDAV 模式仅保留「保存设置 + 备份管理」，移除与备份冲突的「立即同步/从云端恢复」（Git 模式保留）
- **备份列表状态**：WebDAV 未配置时明确显示「✗ WebDAV 未配置」

### 🔧 其他

- 补丁脚本 `patch-aionui.js` 改为基于 `%USERPROFILE%` 动态路径（不再硬编码用户名）
- 手动更新脚本自动检测/安装 pnpm
- 打包跳过 native rebuild（纯 JS 应用，`npmRebuild: false`）

---

## v1.0.0（初始版本）

- 窗口内嵌 DSH Web UI、底部状态栏（模型/用量/费用/余额）
- 右侧面板「概览」标签、归档会话取消归档
- 托盘常驻、关闭最小化到任务栏、退出自动停止服务
- 云端快照备份（WebDAV/Git）、一键安装（Node.js/dsh 环境自检）
- dsh-web-ui 全家桶（皮肤中心 / 任务看板 / 宠物）集成
